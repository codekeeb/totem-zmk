# Left half = split central.
#
# Studio itself is enabled from build.yaml. Locking is what makes the
# &studio_unlock bound on LOWER + Z meaningful: without it the keymap is
# editable the moment a client connects, and the unlock key does nothing.
CONFIG_ZMK_STUDIO_LOCKING=y
